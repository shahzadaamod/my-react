import React from 'react';

const SliderTitleItem = ({ title }) => {
  return (
    <div className="views-row">
      <div className="views-field">{title}</div>
    </div>
  );
};

export default SliderTitleItem;
